# Rust
/target/
**/target/
*.pdb

# Cargo vendoring (generated by scripts/vendor-crates.sh for offline OBS builds)
/vendor/
/.cargo/config.toml

# Sailfish SDK / RPM build output
/RPMS/
/SRPMS/
/BUILD/
/BUILDROOT/
*.rpm

# insta snapshot review leftovers
*.snap.new

# cargo-fuzz
/crates/vuo-core/fuzz/target/
/crates/vuo-core/fuzz/corpus/
/crates/vuo-core/fuzz/artifacts/

# Editors / OS
*.swp
*~
.DS_Store
/.idea/
/.vscode/

# Local secrets used by the opt-in live integration test
.env

# Reference checkouts of other projects, cloned locally for study only.
# They are GPL third-party code and must never become part of this tree.
/fuoten/
/sm/
/reference/

# Generated by scripts/patch-deps.sh from patches/ plus the pristine crates.
# The four changed lines live in patches/; see PATCHES.md.
third_party/

# Crate tarballs scripts/patch-deps.sh downloads when neither pristine/ nor the
# cargo registry has them (a fresh CI checkout). Checksum-verified on arrival.
.patch-deps-cache/
