import QtQuick 2.6
// DockedPanel's edge enum. A QML `enum`, not properties: QML property names
// may not begin with an upper-case letter. Same pattern as PageStatus.qml.
QtObject { enum Value { Top = 0, Bottom = 1, Left = 2, Right = 3 } }
