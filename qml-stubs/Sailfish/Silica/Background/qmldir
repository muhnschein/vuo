# A stub of Sailfish.Silica.Background, for linting Vuo's QML on a machine
# without the SailfishOS SDK. See ../qmldir for the rules these follow.
#
# One type is used: ThemeBackground, the item that paints the current
# ambience's background. EntryListPage puts one behind the pinned tab strip so
# the strip is opaque and matches the wallpaper; see the note there for why
# nothing in the public Sailfish.Silica module can do that job.
module Sailfish.Silica.Background

ThemeBackground 1.0 ThemeBackground.qml
