import QtQuick 2.6
import Sailfish.Silica 1.0
// The real one is a Page carrying `__sailfish_webviewpage`, which a WebView
// walks up to so it can follow the page's orientation and status.
Page {
    property int __sailfish_webviewpage
}
